apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    jcenter()
}


dependencies {
	runtime files(fileTree(dir: './lib', include: '*.jar'))
    testCompile 'junit:junit:4.12'
}

sourceSets {
    main {
        java {
            srcDirs = ["src"]
        }
    }
}
